package Operators;

public class ConditionalOp1 {

	/*
	 * 1.It is ternary operator which is used for decision making purpose.
	 * 2.syntax: Condition ? val/exp : val/exp
	 * 3.condition is first operand after ? val/exp are second operand and afte colon third operand.
	 * 4.Operand 1 must be boolean it means it should return boolean output
	 */
}
